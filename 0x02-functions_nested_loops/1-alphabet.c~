#include "holberton.h"
/**
 *Description: print letters from a - z in lowercase
 *
 *Return: 0 if successful
 **/
void print_alphabet(void)
{
  int letter = 'a';

  while (letter <= 'z')
    {
      _putchar(letter);
	letter++;
    }
  _putchar('\n');
  return (0);
}
