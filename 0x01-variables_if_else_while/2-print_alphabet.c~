#include <stdio.h>
/**
*main - where execution starts
*
*Description: The program will print the alphabets from a-z
*Return: 0 if successful
**/
int main(void)
{
char ch = 'a'

while (ch <= 'z')
{
putchar(ch);
ch++
}
putchar('\n');
return (0);
}
